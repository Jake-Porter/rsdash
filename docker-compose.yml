version: "3.8"
services:
  rsdash:
    build: .
    container_name: ctnr-rsdash-01
    restart: unless-stopped
    ports:
      - "8083:8000"
    environment:
      - TZ=America/Denver
      - RSN_RS3=${RSN_RS3:-}
      - RSN_OSRS=${RSN_OSRS:-}
      - HISCORES_POLL_MINUTES=${HISCORES_POLL_MINUTES:-30}
      - FEED_POLL_MINUTES=${FEED_POLL_MINUTES:-15}
    volumes:
      - /opt/rsdash/data:/app/data
